# Scripts Workspace

This directory is reserved for external tooling that supports the project outside the Godot editor.

Examples:

- import helpers
- data generators
- localization tooling
- build or export helpers
